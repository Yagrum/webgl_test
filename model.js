class Model
{
    constructor(id, VBO, IBO, UVBO, numVertices, numIndices)
    {
      this.id = id;
      this.VBO = VBO;
      this.IBO = IBO;
      this.UVBO = UVBO;
      this.numVertices = numVertices;
      this.numIndices = numIndices;
    }
}
